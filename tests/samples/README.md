Local host-test modules. Not shipped in the release zip.

- `doom-hanger.dmf` — DefleMask Genesis (copied from xmp-dmf tests).
- `quickstart.fur` / `LedStorm.fur` — Furnace demo songs (all rights reserved
  to their authors; used only for local verification, not redistributed).
- `reject-ddmf.dmf` — synthetic `DDMF` header; must be rejected.
